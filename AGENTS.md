# AGENTS.md

本仓库的 AI 协作约定。

## 先读这个 skill

`.claude/skills/rm-vision-onboarding/SKILL.md`

内容是**仓库主人的背景和协作方式**：他是电控（STM32/C/串口）转视觉，
图像和数学基础薄弱，需要大白话解释和明确的第一步第二步。
里面还有这个工程的关键事实（相机独占、参数存放位置、编译运行流程）
和一批真实踩过的坑。

**涉及相机调参、装甲板识别、参数判定、编译运行、SSH 操作小电脑时，
先读它再动手。** 尤其是判断"参数合格吗"这类问题——判据必须来自代码
和历史配置，不能凭通用常识（这个错犯过，代价是两轮无效返工）。

## 工程资料

| 文件 | 内容 |
| --- | --- |
| `自瞄代码导读.md` | 60KB，写给电控转算法的人。分 0-6 阶段，每阶段有可动手验证的项 |
| `怎么编译运行.md` | 编译运行流程，含「为什么运行前必须 cd build」 |
| `readme.md` | 工程总览 |

回答原理性问题时先看《自瞄代码导读》是否已覆盖，指向对应阶段，
再补充卡住的那一点。不要重复造解释。

## 硬约束

- 所有程序**必须先 `cd build` 再跑**，程序内部用 `../configs/xxx.yaml` 相对路径
- 相机**独占访问**，同一时刻只能一个程序开着；冲突报 `-0x3ec`
- 源文件增删或改了 `CMakeLists.txt` 后要重新 `cmake -S . -B build`，光 `make` 不够
- 只有 `configs/calibration.yaml` 是生效的参数文件；`configs/tuning_result.yaml`
  是给人抄的中间结果，程序不读
- 不擅自提交（`git commit`），除非明确要求
