# 怎么编译运行（速查）

快捷键和你 STM32 电控工程一致。忘了就看这一页。

---

## 快捷键

| 快捷键 | 视觉工程 | 你电控工程里是 |
| --- | --- | --- |
| `Alt+,` | 编译 | 编译 |
| `Alt+N` | **运行**（跑离线视频） | 烧录进 C 板 |
| `Alt+M` | 编译并运行 | 编译并烧录 |

只有 `Alt+N` 含义不同。视觉程序直接在电脑上跑，没有"烧录"这一步。

**日常就按 `Alt+M`**：改完代码按一下，自动编译 + 弹窗看效果。

---

## 首次使用：配一次快捷键

和电控工程同样做法（那边指南第 7 节）：

`Ctrl+Shift+P` → `Preferences: Open Keyboard Shortcuts (JSON)`

加入下面三段（电控那边已配过 `Alt+,` 的话，那段不用重复加）：

```json
{
    "key": "alt+oem_comma",
    "command": "workbench.action.tasks.runTask",
    "args": "build"
},
{
    "key": "alt+n",
    "command": "workbench.action.tasks.runTask",
    "args": "run"
},
{
    "key": "alt+m",
    "command": "workbench.action.tasks.runTask",
    "args": "build and run"
}
```

`alt+oem_comma` 就是 `Alt+,`。`args` 要和 `tasks.json` 里的 `label` 一字不差。

注意 `args` 和电控工程不同（`run` 而不是 `flash`）。`runTask` 是在当前工作区里
找 label，所以两个工程用同一套快捷键不冲突 —— 打开哪个工程就跑哪个的 task。

> 同一个坑：VSCode 每个 profile 有各自的 keybindings.json，写在别的 profile 里
> 没用。电控指南第 15 节讲过这个。

---

## 前提：必须用 WSL 打开项目

VS Code 左下角要显示绿色的 **`WSL: Ubuntu-22.04`**。

没有的话：左下角按钮 → `Connect to WSL` → 打开文件夹 → 输入

```
/mnt/d/RM27_zimiao/27zm_zy_20260826
```

Windows 的 `D:\` 在 Linux 里是 `/mnt/d/`，反斜杠换正斜杠，其余一样。

**为什么必须用 WSL**：OpenCV / Eigen / OpenVINO 装在 WSL 里，Windows 侧没有。
直接开 `D:\...` 会满屏红波浪线，而且编译不了。

这点和电控工程不同 —— 那边用 Windows 上的 ARM 交叉编译器，直接打开就行。

---

## 六个任务

`Ctrl+Shift+P` → 输入 `run task` → 回车，能看到完整列表：

| label | 干什么 | 什么时候用 |
| --- | --- | --- |
| `build` | 增量编译，几秒 | 日常（`Alt+,`） |
| `run` | 跑离线测试 | 只想看效果（`Alt+N`） |
| `build and run` | 编译+运行 | **最常用**（`Alt+M`） |
| `rebuild` | 重新 cmake 配置再编译 | 新增/删源文件、改了 CMakeLists.txt |
| `run range` | 只跑视频某一段 | 反复调某段画面，会问起止帧号 |
| `clean rebuild` | 删 build 重来 | 编译出诡异问题时 |

---

## 编译报错怎么看

和电控工程一样挂了 `$gcc` 的 problemMatcher：

- `Ctrl+Shift+M` 打开"问题"面板，报错列在里面
- 终端里的 `文件:行:列` 可以 `Ctrl+点击` 直接跳到出错那行

---

## 如果想在终端里敲

按 **Ctrl+`**（反引号，Tab 上面那个键）打开终端：

```bash
# 编译
make -C build -j18

# 运行（必须先 cd build）
cd build
./auto_aim_test
```

命令的意思：

| 命令 | 人话 |
| --- | --- |
| `cd 路径` | 进那个文件夹（change directory） |
| `make -C build -j18` | 在 build 文件夹里编译，用 18 核并行 |
| `./auto_aim_test` | 运行当前文件夹里的这个程序（`./` = 当前文件夹里的） |
| `bash make.sh` | 执行 make.sh 脚本（相当于双击 .bat） |

### 为什么运行前必须 cd build

程序内部找配置写的是 `../configs/demo.yaml`，`..` = 上一层文件夹。

- 在 `build` 里跑 → `..` 指向项目根 → 找得到
- 在项目根跑 → `..` 指向 `RM27_zimiao` → 找不到，直接退出

用 `Alt+M` 就不用管这个，工作目录已经配在 task 里了。

---

## 九个程序，你能跑哪些

| 程序 | 干什么 | 在你笔记本上 |
| --- | --- | --- |
| `auto_aim_test` | 读录像跑完整自瞄链路，弹窗显示 | 能跑 |
| `split_video` | 视频抽帧裁剪 | 能跑 |
| `rb_auto_aim_debug` | 上场主程序 | 不能，要相机 + 串口 |
| `test_simple` / `capture` / `calibrate_*` | 相机测试与标定 | 不能，要真相机 |

后面几个会调用大恒相机 SDK。本机装的是占位库（函数返回错误码），
必须在车上的小电脑跑。

---

## 三个常见报错

**`[YAML] xxx not found!` 然后程序立刻退出**

配置文件缺 `xxx` 这个键。`tools/yaml.hpp` 读不到键时直接 `exit(1)`，不抛异常，
所以看起来像莫名死掉。照报错名字去 `configs/*.yaml` 补上，
可以对照另一个能跑的配置文件抄。

**找不到配置文件 / 视频**

没在 `build` 目录里运行。用 `Alt+M` 不会有这问题。

**改了代码但行为没变**

忘了重新编译。`Alt+,` 或 `Alt+M`。

---

## 窗口显示不出来

本机 WSLg 需要两个设置才能正常显示 OpenCV 窗口，已写进 `~/.profile`：

```bash
unset WAYLAND_DISPLAY          # OpenCV 的 Wayland 后端在 WSLg 上渲染不出画面
export LIBGL_ALWAYS_SOFTWARE=1 # 用软件渲染，2D 绘制够用且更稳
```

如果窗口又不显示：在 PowerShell 里执行 `wsl --shutdown`，然后重开终端。
WSLg 的 X server 偶尔会卡住，重启能恢复。

验证图形是否正常：终端敲 `xeyes`，应弹出一双跟着鼠标转的眼睛。

---

## 环境里装了什么（备查）

| 组件 | 版本 | 位置 |
| --- | --- | --- |
| Ubuntu | 22.04.5 LTS | WSL2 |
| g++ | 11.4.0 | 系统 |
| cmake | 3.22.1 | 系统 |
| OpenCV | 4.5.4 | `/usr/include/opencv4` |
| Eigen | 3.4 | `/usr/include/eigen3` |
| OpenVINO | 2024.6.0 | `/opt/intel/openvino_2024.6.0` |
| clangd | 14.0.0 | `/usr/bin/clangd` |

`libgxiapi.so`（大恒相机 SDK）是**占位库**，在 `/usr/local/lib/`，
函数全部返回错误码。上车前必须去大恒官网装真 SDK。

---

## 和电控工程的差异汇总

| | 电控（STM32） | 视觉 |
| --- | --- | --- |
| 在哪编译 | Windows 直接编 | **必须在 WSL 里** |
| 编译器 | arm-none-eabi-gcc（交叉编译） | g++（本机） |
| 构建系统 | CMake + Ninja + Preset | CMake + Make |
| 产物 | `.elf` / `.hex` / `.bin` | 9 个可执行文件在 `build/` |
| `Alt+N` | 烧录进 C 板（OpenOCD / J-Link） | 直接运行 |
| 看结果 | 车上的现象 + Ozone 调试 | 弹窗画面 + 终端日志 + PlotJuggler 曲线 |
| 依赖位置 | 拷进仓库（`Drivers/`、`RM2023_Lib/`） | 装在系统里（`/usr/include`） |
