# 自瞄代码导读

写给**电控转算法**的人看。假定你熟悉 STM32、C、串口调试、交叉编译，
但卡尔曼滤波、PnP、四元数只听过名字。目标不是"能从零手写自瞄"，而是
**能看懂整条链路 + 知道哪个参数该往哪调**。一两周的量。

---

## 这份笔记怎么用

**主轴是"自外向内"**：从你已经熟的串口收发包切进去，再往里走图像和数学。
不要跳着读，后面的阶段依赖前面的结论。

| 阶段 | 读什么 | 你能做的验证 |
| --- | --- | --- |
| 0 跑起来 | 无 | 编译 + 跑通离线视频 + 接上 PlotJuggler |
| 1 通信协议 | `io/gimbal/gimbal.hpp` | 对着结构体数字节，判断出 crc16 的问题 |
| 2 链路骨架 | `tests/auto_aim_test.cpp` | 说出 `cv::Mat -> list<Armor> -> list<Target> -> io::Command` |
| 3 识别 | `tasks/auto_aim/detector.cpp`、`armor.hpp` | 调 `threshold` 看二值化窗口变化 |
| 4 坐标与弹道 | `tasks/auto_aim/solver.cpp`、`tools/trajectory.cpp` | 看 `armor_x/armor_y` 曲线是否合理 |
| 5 跟踪与 EKF | `tasks/auto_aim/target.cpp`、`tracker.cpp` | 看 11 条状态曲线，调跟踪计数 |
| 6 收口 | `tools/extended_kalman_filter.cpp` | 改 NIS 阈值前后对比 |

**三个约定**：

1. 本文所有 `文件:行号` 都是照着当前工作区实测的。代码一改行号就漂，
   引用对不上时**以 grep 到的函数名为准**，别信行号。
2. 所有程序**必须先 `cd build` 再跑**。原因见 `怎么编译运行.md` 的
   「为什么运行前必须 cd build」。
3. 命令行参数**必须写成 `-s=100`，不能写 `-s 100`**。原因见下面阶段 0。

编译运行的快捷键、报错速查、环境清单在 `怎么编译运行.md`，本文不重复。

---

# 阶段 0：先跑起来

看不到东西就学不下去。这一阶段只有一个目标：**让画面和曲线动起来**。

## 编译运行速查

```bash
# 编译（仓库根）
bash make.sh
# 或者只增量编译
make -C build -j18

# 运行（必须先 cd build）
cd build
./auto_aim_test                 # 跑整段 demo 视频
./auto_aim_test -s=0 -e=30      # 只跑第 0~30 帧
```

跑起来应该：弹出一个叫 `reprojection` 的窗口显示装甲板视频，
另外还有一个 `binary_img` 二值化窗口，终端每帧打印一行

```
[12] yolo: 18.4ms, tracker: 0.4ms, aimer: 0.0ms
```

窗口不显示，先执行这两行再重试（原因见 `怎么编译运行.md` 的「窗口显示不出来」）：

```bash
unset WAYLAND_DISPLAY
export LIBGL_ALWAYS_SOFTWARE=1
```

## `-s=100` 和 `-s 100` 的区别（第一个坑）

参数定义在 `tests/auto_aim_test.cpp:20-24`，用的是 OpenCV 的 `CommandLineParser`：

```
"{config-path c  | ../configs/demo.yaml | yaml配置文件的路径}"
"{start-index s  | 0                    | 视频起始帧下标    }"
"{end-index e    | 0                    | 视频结束帧下标    }"
"{@input-path    | ../assets/demo/demo  | avi和txt文件的路径}"
```

`@input-path` 是**位置参数**。如果你写 `-s 100`，OpenCV 不把 `100` 当成 `-s` 的值，
而是当成第一个位置参数，于是程序去找 `100.avi`：结果只报几行 GStreamer 警告然后退出，
不会告诉你"参数写错了"。

**必须用等号**：`-s=100 -e=200`。

> 这条坑原先也埋在 `.vscode/tasks.json` 里 —— "run range" 任务写的是空格形式，
> 所以那个任务一直是坏的。现在已修好（`tasks.json:157`），可以直接用。

## 接上 PlotJuggler（看曲线，后面阶段 5 全靠它）

程序把内部状态以 JSON 打成 UDP 包发出去，PlotJuggler 负责画。
发送端是 `tools::Plotter`，默认目标 `127.0.0.1:9870`（`tools/plotter.hpp:15`）。

```bash
sudo apt install -y plotjuggler
plotjuggler
```

接入步骤：

1. 左上 Streaming 下拉选 **UDP Server**，点 Start
2. Port 填 **9870**，Message Protocol 选 **JSON**
3. 确定后另开一个终端 `cd build && ./auto_aim_test`
4. 左侧 Timeseries List 会冒出一堆变量名，拖到右边画布上就开始画

这一步现在就配好，别等到阶段 5。曲线是这套代码唯一的"示波器"，
相当于你在电控那边用的 Ozone / 串口绘图。

## 阶段 0 完成标准

- [ ] `bash make.sh` 零 error
- [ ] `cd build && ./auto_aim_test -s=0 -e=30` 弹窗 + 终端每帧打印，正常退出
- [ ] PlotJuggler 里能看到 `gimbal_yaw`、`armor_num` 这些量在动

---

# 阶段 1：从你已懂的地方切进去（通信协议）

整个仓库里你唯一的现成知识锚点就在这儿：`io/gimbal/gimbal.hpp`。
三个 packed struct，和你在 C 板那边定义的是同一套包。

## 三个通信包

全部带 `__attribute__((packed))`，即关闭对齐填充，让内存布局和串口字节流一一对应，
可以直接 `read()` 进结构体再按字段取用（`gimbal.cpp:258`），不用手工拆字节。
**两边字段顺序必须逐字节对齐**，少一个字段或换个顺序，收到的就是乱码。

### 收包：`GimbalToVision`（C 板 -> 视觉，37 字节）

定义 `gimbal.hpp:33`，解析在 `gimbal.cpp:229` 起的 `read_thread()`。

| 偏移 | 字节 | 字段 | 说明 |
| --- | --- | --- | --- |
| 0 | 2 | `head[2]` = `{0x5a,0x53}` | 帧头。不对就 `flushInput()` 重新同步（`gimbal.cpp:265-273`） |
| 2 | 1 | `mode` | 0 空闲 / 1 自瞄 / 2 小符 / 3 大符 / 4 长焦。转枚举见 `gimbal.cpp:319` |
| 3 | 2 | `color` | **我方**颜色，0 红 / 1 蓝 |
| 5 | 16 | `q[4]` | 云台姿态四元数，wxyz 顺序。**整条链路最关键的输入** |
| 21 | 4 | `bullet_speed` | 弹速 m/s，弹道解算用 |
| 25 | 2 | `bullet_count` | 累计发弹数，靠它跳变判断"是否刚开了一枪" |
| 27 | 4 | `gimbal_yaw` | **无人消费**，见下 |
| 31 | 4 | `gimbal_pitch` | **无人消费**，见下 |
| 35 | 2 | `crc16` | 收包侧**真校验**，失败整帧丢弃（`gimbal.cpp:280`） |

两个要注意的：

- **`gimbal_yaw` / `gimbal_pitch` 读进来就没人再用了。** 上层实际用的角度是由
  `q[4]` 反算的（`gimbal.cpp:289-294` 先转欧拉角，再按 yaml 的轴映射重组）。
  也就是说下位机发的这两个原始角度是冗余字段。
- **`color` 是我方颜色，取反才是敌方**：`gimbal.cpp:313`
  `state_.enemy_color = !rx_data_.color`。收包里 0 红 / 1 蓝，取反后语义也跟着反，
  所以读这行的时候要额外小心（见下面「一处对不上的红蓝映射」）。

### 发包 A：`VisionToGimbal`（29 字节，**当前未使用**）

定义 `gimbal.hpp:71`，填包在 `gimbal.cpp:160` 的 `Gimbal::send()`。

| 偏移 | 字节 | 字段 |
| --- | --- | --- |
| 0 | 1 | `head` = `0x66` |
| 1 | 1 | `mode`（`control ? (fire ? 2 : 1) : 0`，`gimbal.cpp:162`） |
| 2 | 4 | `yaw` 目标绝对角，**弧度** |
| 6 | 4 | `yaw_vel` rad/s |
| 10 | 4 | `yaw_acc` rad/s² |
| 14 | 4 | `pitch` 目标绝对角，**弧度** |
| 18 | 4 | `pitch_vel` rad/s |
| 22 | 4 | `pitch_acc` rad/s² |
| 26 | 2 | `crc16` **从未被赋值** |
| 28 | 1 | `end` = `0x11` |

带速度和加速度是为了让电控侧做**前馈跟随**，不是只给个位置让它自己追。

### 发包 B：`sb_VisionToGimbal`（36 字节，★实际在用的就是这个）

定义 `gimbal.hpp:98`，填包在 `gimbal.cpp:181` 的 `Gimbal::sb_send()`。
前 26 字节与发包 A 完全一样，之后把 `crc16` 换成了目标信息：

| 偏移 | 字节 | 字段 | 说明 |
| --- | --- | --- | --- |
| 26 | 4 | `target_x` | 整车旋转中心世界系 x，米。取自 EKF 状态第 0 维 |
| 30 | 4 | `target_y` | 同上，第 2 维。见 `rb_auto_aim_debug.cpp:101-102` |
| 34 | 1 | `target_name` | 兵种编号，发的是 **`ArmorName + 1`**（`rb_auto_aim_debug.cpp:100`），0 留作"无目标" |
| 35 | 1 | `end` = `0x11` |

**这个包没有任何校验字段**，只靠 `head`(0x66) + `end`(0x11) 兜着。

上场路径：`Planner`(MPC) 产出 `Plan` -> `rb_auto_aim_debug.cpp:107` -> `sb_send()`。
全仓库只有 `rb_auto_aim_debug.cpp:107` 和 `:312` 两个发送点，调的都是 `sb_send()`。

## `GimbalState`：给算法层用的"已解析"形式

定义 `gimbal.hpp:134`。**它不是通信包**，是收包后解析换算好的内部状态，
上层通过 `Gimbal::state()` 拿快照（带 mutex）。填写位置 `gimbal.cpp:306-315`。

| 字段 | 单位 | 有没有人写 |
| --- | --- | --- |
| `yaw` | **度**（`gimbal.cpp:308` 乘了 57.3） | 有 |
| `pitch` | **度**（`gimbal.cpp:309` 乘了 57.3） | 有 |
| `yaw_vel` / `pitch_vel` | — | **无人写入**，恒为初值。下位机没发速度 |
| `q2yaw` / `q2pitch` | — | **无人写入** |
| `mode` | — | 有（`gimbal.cpp:312`） |
| `enemy_color` | 0 蓝 / 1 红 | 有，**取反得到**（`gimbal.cpp:313`） |
| `bullet_speed` | m/s | 有 |
| `bullet_count` | 次 | 有 |

### ★单位陷阱（全仓库最容易错的一处）

- `GimbalState.yaw/pitch` 是**度**
- 发送包里的 `yaw/pitch` 是**弧度**
- **算法内部一律弧度**

所以 `rb_auto_aim_debug.cpp:312` 那个收尾调用要把 `current_state.yaw / 57.3f`
转回弧度再发。看到 57.3 就是在做弧度/度转换（180/π ≈ 57.3）。

## 波特率 460800，但其实无所谓

`gimbal.cpp:24` 硬编码 `serial_.setBaudrate(460800)`，不在 yaml 里。

端口是 `/dev/ttyACM*` 这类 **USB-CDC** 虚拟串口。它底层跑的是 USB 批量传输，
波特率只是个"名义值"，两边不一致也能通 —— 和你在电控那边调 UART 真实波特率
必须严格匹配不一样。所以**串口连不上时，不要去怀疑波特率**，先查设备名和权限。

## crc16 缺失的完整说明

这是三处疑似 bug 里最能练"读代码下判断"的一处，正好落在你的知识边界上。

事实链（自己 grep 一遍再往下看）：

1. `VisionToGimbal.crc16` 声明了（`gimbal.hpp:83`），但**全仓库没有任何一处给它赋值**。
2. 唯一的 `get_crc16` 调用在 `gimbal.cpp:219-220`，**整段被注释掉了**。
3. `gimbal.cpp:148` 和 `:169` 各有一条
   `reinterpret_cast<uint8_t *>(&tx_data_), sizeof(tx_data_);`
   —— 这是**逗号表达式构成的空语句**，算完就丢，没有任何效果。
   大概是当初删 crc16 计算时留下的残骸。编译器通常只给个 warning。
4. 实际在用的 `sb_VisionToGimbal` **连 `crc16` 字段都没有**。
5. 而**接收侧是真校验的**：`gimbal.cpp:280` 调 `check_crc16`，失败就整帧丢弃。

结论：**校验只做了单向**。下位机发给视觉的包有 CRC 保护，视觉发给下位机的包没有。
一旦串口受干扰导致某个字节翻转，下位机会拿着一个错误的 yaw/pitch 去转云台，
而且没有任何机制能发现。

### 为什么不动手改它

改它要电控侧固件配合：两边的校验算法、包长必须**同时**改，
否则包长一变，下位机按老结构解析，收到的全是错位数据 —— 比没有校验更糟。
而你现在没车没 C 板，**做不了闭环验证**，改了只能是"看起来对"。

所以这一处**只做"读懂并判断"**。真要改，流程是：先和电控确认包结构，
两边同时改，然后在车上用示波器/逻辑分析仪抓一帧确认。

## 一处对不上的红蓝映射（读代码时能自己发现的）

`enemy_color` 配成 `"auto"` 时，两个 track 方法的映射**是相反的**：

| 位置 | 代码 |
| --- | --- |
| `tracker.cpp:61`（`sb_track`） | `enemy_color_ = (g.enemy_color == 0) ? Color::blue : Color::red` |
| `tracker.cpp:136`（`track`） | `enemy_color_ = (g.enemy_color == 0) ? Color::red : Color::blue` |

同一个输入，两个函数给出相反的结论，**至少有一个是错的**。
结合上面 `enemy_color = !rx_data_.color` 的取反，这里很容易绕晕。

但这要**接 C 板才能验证**（离线时 `enemy_color` 用的是 yaml 里写死的
`"blue"`，`configs/demo.yaml:2`，压根走不到 `"auto"` 分支），
所以同样只记录、不动手改。

## 三种"命令"类型别混淆

链路末端有三个长得像的东西，谁产生谁消费必须分清：

| | `io::Command` | `auto_aim::Plan` | `io::GimbalState` |
| --- | --- | --- | --- |
| 定义 | `io/command.hpp:6` | `tasks/auto_aim/planner/planner.hpp:20` | `io/gimbal/gimbal.hpp:134` |
| 方向 | 视觉内部产出 | 视觉内部产出 | 下位机 -> 视觉 |
| 谁产生 | `Aimer::aim()`（`aimer.cpp:32`） | `Planner::plan()` | `Gimbal::read_thread()` |
| 谁消费 | **只有离线测试**打印/画曲线 | `rb_auto_aim_debug.cpp:107` -> `sb_send()` | 上层 `gimbal.state()` |
| 字段 | `control` `shoot` `yaw` `pitch` `horizon_distance` | `control` `fire` `target_yaw` `target_pitch` `yaw` `yaw_vel` `yaw_acc` `pitch` `pitch_vel` `pitch_acc` | 见上面 GimbalState 表 |
| 是否上场 | ❌ 不发送 | ✅ **上场路径** | ✅ |

三句话记住：

- `io::Command` 只有位置，**没有速度前馈**，而且离线测试里算出来就扔了，从不发送。
- `auto_aim::Plan` 多带速度和加速度（MPC 的输出），**这才是真正发给车的**。
- `GimbalState` 是反方向的，是输入不是输出。

## 阶段 1 完成标准

- [ ] 能对着 `gimbal.hpp` 说出三个包各多少字节、哪个是实际在用的
- [ ] 能解释为什么 `GimbalState.yaw` 是度而发送包是弧度
- [ ] 能独立讲清 crc16 的问题以及为什么现在不改
- [ ] 知道 `io::Command` 和 `auto_aim::Plan` 哪个才是上场路径

---

# 阶段 2：主链路骨架

先有全局图，再钻细节。这一阶段不要试图看懂任何数学。

## 一句话记住整条链路

```
cv::Mat  --detect-->  list<Armor>  --track-->  list<Target>  --aim-->  io::Command
  一帧图              若干装甲板         整车目标            云台指令
```

记住这一串就等于记住了全仓库的骨架。注释也写在
`tests/auto_aim_test.cpp:83-85`。

## 七个阶段（照着 `auto_aim_test.cpp` 走一遍）

离线测试 `tests/auto_aim_test.cpp` 是**最好的入口**：单线程 for 循环，
一眼能看完，而且是本机唯一能真跑的完整链路。

| # | 干什么 | 类 / 文件 | 调用点 | 入 -> 出 |
| --- | --- | --- | --- | --- |
| 1 | 读一帧图 + 对应姿态 | `cv::VideoCapture` + `demo.txt` | `auto_aim_test.cpp:43-46` | 文件 -> `cv::Mat` + 四元数 |
| 2 | 喂云台姿态 | `Solver::set_R_gimbal2world` | `:92` | 四元数 -> 存进 solver 内部 |
| 3 | 识别 | `YOLO::detect` | `:100` | `cv::Mat` -> `list<Armor>` |
| 4 | 解算（在 detect 内部完成） | `Solver::solve` | `solver.cpp:101` | `Armor&` -> 填上 `xyz_in_world` |
| 5 | 跟踪 | `Tracker::test_track` | `:110` | `list<Armor>` -> `list<Target>` |
| 6 | 火控 | `Aimer::aim` | `:117` | `list<Target>` -> `io::Command` |
| 7 | 输出曲线 | `tools::Plotter::plot` | `:224` | JSON -> UDP 9870 |

**顺序不能反**：必须先 `set_R_gimbal2world` 再 `detect`，
因为算世界坐标要用这一帧的姿态。

第 2 步那个四元数**来自 `demo.txt`**（录制时同步存下的电控回传姿态），不是算出来的。
所以离线跑的时候，"云台"是录像里的历史姿态，你改不了。

## ★两条链路的差异（全仓库最容易误解的一点）

仓库里有**两个**跑自瞄的程序，走的路**不一样**：

| 环节 | `auto_aim_test`（本机能跑） | `rb_auto_aim_debug`（上场） |
| --- | --- | --- |
| 识别 | **YOLO** `yolo.detect`（`auto_aim_test.cpp:100`） | **传统CV** `detector.detect`（`rb_auto_aim_debug.cpp:180`） |
| 跟踪 | `tracker.test_track`（`:110`） | `tracker.track`（`:181`） |
| 火控 | `Aimer` -> `io::Command`，**算完不发送**（`:117`） | `Planner` MPC -> `Plan` -> `gimbal.sb_send`（`:107`） |
| 线程 | 单线程 for 循环 | 视觉线程 + 200Hz `plan_thread` |
| 云台姿态 | 来自 `demo.txt` 录像 | 来自串口实时回传 |

### 主程序构造了 YOLO 却从未调用

这是最反直觉的一点，自己 grep 验证：

- `rb_auto_aim_debug.cpp:49` 构造 `auto_aim::YOLO yolo(config_path, true)`，
  这一步会**加载并编译 OpenVINO 模型**（很花时间）
- 然后全文件**没有任何一次 `yolo.detect` 调用**
- `:180` 调的是 `detector.detect(img)`，即传统CV
- 全仓库唯一的 `yolo.detect` 在 `tests/auto_aim_test.cpp:100`，也就是离线那条路

**这意味着：在离线视频里调好的识别参数，不一定是车上跑的那条路。**
调参之前先确认自己在调哪条链路。

### 但两条路也不是完全隔离

`tasks/auto_aim/yolos/yolov5.cpp:184`：当 `use_traditional_` 为真时，
YOLO 检测完还会**回头调传统CV 精修角点**（调的是 `detector.cpp` 里
`detect(Armor&, const cv::Mat&)` 那个重载）。

而两个配置都开着这个开关：`configs/demo.yaml:12`、`configs/sb_long.yaml:18`
都是 `use_traditional: true`（读入点 `yolov5.cpp:27`）。

所以：**`detector.cpp` 的代码两条路都会碰到**，值得认真读。

## `tracker` 三个 track 方法并存

`tasks/auto_aim/tracker.cpp` 里有三个几乎一样的方法，最容易看晕：

| 方法 | 定义行 | 用途 | 离线可用？ |
| --- | --- | --- | --- |
| `sb_track` | `:49` | 哨兵专用。**唯一真正按 `ArmorPriority` 排序**的（`:90`） | ❌ `:55-59` 解引用 `gimbal_` |
| `track` | `:126` | 上场步兵用。多一段"按右键切换/锁定目标"逻辑 | ❌ `:130-134` 同样要 `gimbal_` |
| `test_track` | `:232` | 离线测试用。**完全不碰 `gimbal_`** | ✅ 笔记本跑的就是它 |

`test_track` 存在的唯一理由就是**不依赖云台**。代价是拿不到下位机状态，
`enemy_color` 只能用 yaml 里写死的值。

另两处差异会咬人：

1. **按优先级排序只有 `sb_track` 是活的**。`track`（`:164-166`）和
   `test_track`（`:263-265`）那几行都被注释掉了。
2. **`Armor::priority` 从未被赋值**（见下面死代码清单），所以"优先级"目前形同虚设。

## 四层目录职责

没有 ROS，没有中间件，就是普通的分层 C++ 工程。

| 层 | 目录 | 职责 | 能不能反向依赖 |
| --- | --- | --- | --- |
| 应用 | `src/` | 上场主程序，把各层串起来 | — |
| 算法 | `tasks/auto_aim/` | 识别、解算、跟踪、火控、MPC | 不依赖 `src/` |
| 硬件 | `io/` | 相机、串口云台、CAN | 不依赖 `tasks/` |
| 工具 | `tools/` | EKF、弹道、绘图、日志、yaml | 谁都能用 |

另有 `tests/`（离线测试）、`calibration/`（标定与录像工具）、`configs/`（yaml）、
`assets/`（模型与 demo 视频）。

依赖方向是单向的：`src/` -> `tasks/` -> `io/` -> `tools/`。
`tasks/auto_aim` 的编译配置在 `tasks/auto_aim/CMakeLists.txt`（**不是根 CMakeLists**）。

## 九个可执行文件，本机只有两个能跑

`add_executable` 全在根 `CMakeLists.txt:91-130`：

| 程序 | 干什么 | 本机 |
| --- | --- | --- |
| `auto_aim_test` | 读录像跑完整自瞄链路 | ✅ 能跑 |
| `split_video` | 视频抽帧裁剪 | ✅ 能跑 |
| `rb_auto_aim_debug` | 上场主程序 | ❌ 要相机 + 串口 |
| `capture` | 采图录像 | ❌ 要相机 |
| `calibrate_camera` | 相机内参标定 | ❌ 要相机 |
| `calibrate_handeye` | 手眼标定 | ❌ 要相机 + 车 |
| `calibrate_robotworld_handeye` | 手眼标定（另一种） | ❌ 同上 |
| `calibrate_input` | 标定输入工具 | ❌ 要相机 |
| `test_simple` | 相机连通性测试 | ❌ 要相机 |

**为什么不能跑**：这些程序都会调大恒相机 SDK，而本机
`/usr/local/lib/libgxiapi.so` 是**占位库**，函数全部返回错误码
（链接位置 `io/CMakeLists.txt:89`）。详见 `怎么编译运行.md` 的
「九个程序，你能跑哪些」。

## ★死代码清单（别读，省时间）

这些代码会被编译，但**运行时永远不会执行**。新人最容易在这里浪费几天。

| 内容 | 状态 | 证据 |
| --- | --- | --- |
| `rv_detector.cpp` / `rv_number.cpp` | 编译进 `auto_aim`，但**自身之外零引用**，约 567 行 | `tasks/auto_aim/CMakeLists.txt:37-38`；全仓库无 `RVDetector`/`RVNumber` 外部引用 |
| `voter.cpp` | **从不实例化** | 全仓库无 `Voter` 构造 |
| `shooter.cpp` | **从不实例化**，火控实际在 `Planner` | 全仓库无 `Shooter` 构造 |
| `io/cboard.cpp`（CAN） | **从不实例化**，实际走串口 `io/gimbal/` | `calibration/capture.cpp:31,155` 两处构造都被注释 |
| `tools::Recorder` | **从不实例化**。只在 `rb_auto_aim_debug.cpp:23` 被 `#include`，无任何构造或调用 | 全仓库无 `Recorder` 对象 |
| `Camera::timestamp_offset` | yaml 读入但**无消费者** | `io/camera.cpp:17-18` 读入，`camera.hpp:36` 声明，无人使用 |
| `Armor::priority` | **从未被写入**，只被读 | 声明 `armor.hpp:146`；唯一"写"是 `target.cpp:31` 复制走；排序只有 `tracker.cpp:90` |
| `GimbalToVision.gimbal_yaw/pitch` | 读进来**无人消费** | 实际角度由 `q[4]` 反算 |
| `GimbalState.yaw_vel/pitch_vel/q2yaw/q2pitch` | **无人写入** | 见阶段 1 的 GimbalState 表 |
| configs 里 `buff_detector` / `model` / `buff_aimer` 全部键 | 能量机关遗留，**无消费者** | `configs/sb_long.yaml:170-195`、`demo.yaml:128-153` |
| `assets/yolo11_buff_int8.xml`、`standard_fanblade.jpg` | 配置引用但**文件不存在** | `demo.yaml:150,152` |
| `TrajectoryV1` | 被 `TrajectoryV2` 取代 | `tools/trajectory.hpp:29` `using Trajectory = TrajectoryV2` |

**不要删它们。** 删除需要先和队里确认有没有在飞的分支依赖 —— 本文只负责标注。

## 阶段 2 完成标准

- [ ] 能默写 `cv::Mat -> list<Armor> -> list<Target> -> io::Command`
- [ ] 能说出上场跑的是传统CV 而不是 YOLO，并知道去哪一行验证
- [ ] 知道三个 track 方法里只有 `test_track` 离线可用，以及为什么
- [ ] 看到 `rv_detector.cpp`、`voter.cpp` 时知道直接跳过

---

# 阶段 3：识别

图像端最直观：改个数字，窗口里立刻能看出区别。这一阶段是**唯一能爽快做实验**的部分，
建议在这里多花时间。

读 `tasks/auto_aim/detector.cpp`（传统CV，上场用的）和
`tasks/auto_aim/armor.hpp`（数据结构）。

## 传统CV 八步流程

`Detector::detect`（`detector.cpp:49`）。每步在源码里都标了 `【第 N 步】`：

| 步 | 干什么 | 行 | 相关 yaml 参数 |
| --- | --- | --- | --- |
| 1 | 转灰度 | `:51` | — |
| 2 | **二值化**（亮度 > 阈值变白） | `:57` | `threshold` |
| 3 | 找轮廓（只取最外层） | `:69` | — |
| 4 | 每个轮廓套最小外接旋转矩形 = 灯条候选，再几何筛选 | `:76` | `min_lightbar_ratio` `max_lightbar_ratio` `min_lightbar_length` `max_angle_error` |
| 5 | 同色灯条**两两配对**成装甲板（O(n²) 全组合） | `:101` | — |
| 6 | 几何筛选装甲板 | `:113` | `min_armor_ratio` `max_armor_ratio` `max_side_ratio` `max_rectangular_error` |
| 7 | 认数字（抠图送分类器） | `:120` | `min_confidence` |
| 8 | 共用灯条去重 | `:136` | — |

**核心思路**：灯条是图像里最亮的东西，所以先二值化把亮的抠出来，
再用"细长"这个几何特征挑出灯条，最后左右配对成板。整个过程不涉及神经网络。

两个顺序上的讲究：

- 第 4 步先筛几何再取颜色 —— `get_color` 要遍历轮廓内像素，比较贵，能便宜滤掉的先滤掉。
- 第 6/7 步先几何筛再认数字 —— 分类器最贵，同理。

所以**第 2 步的阈值放太松，后面全都跟着变慢**：灯条候选一多，
第 5 步的 O(n²) 配对和第 7 步的分类调用都会爆炸。

## 一个必须知道的坑：`binary_img` 窗口是无条件弹出的

`detector.cpp:67` 那句 `cv::imshow("binary_img", binary_img)` **没有 `debug_` 开关**
（本函数末尾的 `show_result` 才受 `debug_` 控制）。

所以你会看到一个自己没要求的黑白窗口。这不是 bug，调 `threshold` 时正好盯着它看。

## 识别参数调参手册

配置在 `configs/demo.yaml`，读入点在 `detector.cpp:34-43`。

### 灯条相关

| 参数 | demo.yaml 值 | 管什么 | 调大 | 调小 |
| --- | --- | --- | --- | --- |
| `threshold` | 150 | 二值化亮度阈值。**第一个该动的旋钮** | 灯条变细甚至断开，远处板找不到 | 反光/白墙/灯带全变白块，误配对暴增 |
| `min_lightbar_ratio` | 1.5 | 灯条最小长宽比 | 更严格，方块状亮斑被滤掉 | 方块也可能被当灯条 |
| `max_lightbar_ratio` | 20 | 灯条最大长宽比 | 允许更细长的 | 细长反光条被滤掉 |
| `min_lightbar_length` | 8 | 最小长度（像素） | 滤掉远处小亮点，但也丢远处目标 | 噪点变多 |
| `max_angle_error` | 45（度） | 允许偏离"竖直"多少 | 允许车侧翻/大倾角 | 只认接近竖直的 |

`max_angle_error` 和 `max_rectangular_error` 在 yaml 里写的是**度**，
读入时 `/57.3` 转弧度（`detector.cpp:35,43`）。

### 装甲板相关

| 参数 | demo.yaml 值 | 管什么 |
| --- | --- | --- |
| `min_armor_ratio` | 1 | 两灯条中点连线 / 长灯条长度，下限 |
| `max_armor_ratio` | 5 | 同上，上限。这个比值也是**区分大小板**的依据 |
| `max_side_ratio` | 1.5 | 长短灯条长度比。正对时 ≈1，侧得越厉害差越多 |
| `max_rectangular_error` | 25（度） | 灯条与连线夹角偏离 90° 多少。真板是矩形，偏差大就是误配对 |
| `min_confidence` | 0.8 | 数字分类置信度下限，低于它被 `check_name` 滤掉 |

### 不在 yaml、改了要重新编译

- **大小板判定阈值 `3.0` / `2.5`**：`detector.cpp:378,385` 的 `get_type`。
  `ratio > 3.0` 判大板，`< 2.5` 判小板，落在中间才按兵种猜（1 号英雄=大板，其余=小板）。

## 一次完整的调参实验（照着做）

目标：找出 `threshold` 的合理范围，并理解它和识别数量的关系。

```bash
cd build
# 1. 先锁定一小段有目标的画面，反复跑同一段才能对比
./auto_aim_test -s=50 -e=80
```

2. 在 PlotJuggler 里拖出 `armor_num`（`auto_aim_test.cpp:144`），这是每帧识别到几块板。
3. 记下当前 `threshold: 150` 时 `armor_num` 的曲线形态。
4. 改 `configs/demo.yaml` 的 `threshold`，依次试 `80` / `150` / `220`，
   每次**重新编译不需要**（yaml 是运行时读的），直接重跑同一段命令。
5. 每次都盯两个东西：`binary_img` 窗口里灯条的完整程度，以及 `armor_num` 曲线。

预期观察：

- `threshold` 太低（80）：二值化图一片白，`armor_num` 抖动甚至出现虚假目标
- 合适（150 附近）：灯条是干净的两根白条，`armor_num` 稳定
- 太高（220）：灯条断成几截或消失，`armor_num` 掉到 0

**注意**：`exposure_us` 和 `threshold` 是耦合的（曝光越高画面越亮），
但**离线视频是录好的，改曝光对回放毫无影响**。所以离线只能调 `threshold`。

## YOLO 与传统CV 怎么复合

别以为两条路完全独立：

1. 离线 `auto_aim_test` 走 `yolo.detect`，用神经网络直接出装甲板和角点。
2. 但 `yolov5.cpp:184`：`use_traditional_` 为真时，YOLO **回头调传统CV 精修角点**
   （调的是 `detector.cpp:183` 那个 `detect(Armor&, const cv::Mat&)` 重载）。
3. 两个配置都开着（`demo.yaml:12`、`sb_long.yaml:18` 都是 `use_traditional: true`）。
4. 上场 `rb_auto_aim_debug` 则**完全走传统CV**。

为什么要精修：YOLO 给的角点是回归出来的，精度大约几个像素；
而 PnP 对角点精度极其敏感（差 1 像素，远处距离能差几十厘米）。
传统CV 的最小外接矩形在灯条清晰时反而更准，所以用它来"擦一遍"。

结论：**`detector.cpp` 的参数两条路都会影响**，值得认真调。

## `Armor` 这个数据结构（识别的输出、跟踪的输入）

定义 `armor.hpp:114`。分两半，**这个划分很重要**：

| 前半段（图像域） | 单位 | 何时有效 |
| --- | --- | --- |
| `color` `left` `right` `center` `points` `ratio` `side_ratio` `rectangular_error` `type` `name` `confidence` | **像素** | detector 出来就有 |

| 后半段（物理域） | 单位 | 何时有效 |
| --- | --- | --- |
| `xyz_in_gimbal` `xyz_in_world` | **米** | 必须等 `solver.solve()` 跑完 |
| `ypr_in_gimbal` `ypr_in_world` `ypd_in_world` `yaw_raw` | **弧度** | 同上 |

**detector 刚吐出来的 `Armor`，后半段是无效的**。这是新人最容易踩的坑之一。

两个字段单独提醒：

- **`center` 不是对角线交点**，是"左右两根灯条中心的中点"。装甲板倾斜或有透视时
  和真正的几何中心有偏差，所以它**只能用于粗筛和排序**（例如按"离图像中心的远近"排序），
  **绝不能当解算输入**。真正参与 PnP 的是 `points` 那 4 个角点。
- **`points` 的顺序是 `{左灯条上, 右灯条上, 右灯条下, 左灯条下}`**，
  必须与 `solver.cpp:37-46` 的两组 3D 物点严格一一对应。
  **错位不会报错**，但会解出完全离谱的位姿。

## 阶段 3 完成标准

- [ ] 能说出八步流程，以及每步对应哪个 yaml 参数
- [ ] 独立完成上面的 `threshold` 实验，能描述三档的现象差异
- [ ] 知道为什么离线改 `exposure_us` 没用
- [ ] 知道 `Armor` 的世界坐标要等 solver 才有效

---

# 阶段 4：坐标与弹道

到这里才需要数学，而且**只讲够用的**。不推导任何公式。

## 最小必要数学补课

只有三个概念，每个都能用你电控侧的经验类比。

### 1. 四元数 —— 你已经在用了

陀螺仪告诉你"现在朝哪、歪多少"。这件事可以用欧拉角（yaw/pitch/roll）表示，
但欧拉角有万向锁、插值不连续的毛病，所以工程上普遍用四个数 `(w,x,y,z)` 的四元数。

**你在电控侧从 IMU 拿到的就是它。** 这里不需要懂它的数学，只要知道：

- 四元数 ≈ 一个"朝向"
- `toRotationMatrix()` 把它变成 3×3 旋转矩阵
- 本仓库的四元数从 `GimbalToVision.q[4]` 进来，wxyz 顺序

**在这个仓库里对应**：`Solver::set_R_gimbal2world`（`solver.cpp:86`）。

### 2. 旋转矩阵与坐标系变换 —— 就是换个参考系说同一个点

同一个物理点，在不同参考系下有不同的坐标值。旋转矩阵就是换算工具：
**"旋转矩阵乘一个点" = 把这个点从一个坐标系换算到另一个坐标系**。

变量名里的 `2` 都读作 "to"：`R_camera2gimbal` = 相机系到云台系的旋转。
连乘就是接力：`R_armor2world = R_gimbal2world * R_camera2gimbal * R_armor2camera`。

电控类比：和你把"电机编码器角度"换算成"机构实际角度"是同一回事，
只不过这里是三维的、用矩阵表示。

**在这个仓库里对应**：`solver.cpp:116-131` 那一串连乘。

### 3. PnP —— 从一张图反推距离和角度

**P**erspective-**n**-**P**oint。已知 n 个点在物体上的**真实三维位置**（这里 n=4，
就是装甲板四个角），又知道它们落在图像上的**像素位置**，
就能反解出"物体相对相机的位置和朝向"。

直观理解：

- 同一块板离得越远，在图上越小 → 从大小能推距离
- 侧过去时四个角会变成梯形 → 从形变能推角度

**它靠"实物多大 + 图上多大"当尺子**，所以实物尺寸填错，解出的距离就整体错。

电控类比：类似用编码器读数反推机构位置，只不过这里的"传感器"是一张图。

**在这个仓库里对应**：`Solver::solve`（`solver.cpp:101`），
调用 `cv::solvePnP(..., cv::SOLVEPNP_IPPE)`（`solver.cpp:110`）。

> 卡尔曼滤波放到阶段 5，而且是先看曲线再看公式。这里不提。

## 四个坐标系

| 坐标系 | 原点 | 朝向 | 特点 |
| --- | --- | --- | --- |
| `armor` | 装甲板中心 | 板面是 `x=0` 平面 | PnP 的物点就定义在这里（`solver.cpp:37-46`） |
| `camera` | 相机光心 | 光轴朝前 | PnP 直接输出的就是相对这个系的位姿 |
| `gimbal` | 云台旋转中心 | 随云台转 | 由标定外参从 camera 变换而来 |
| `world` | **仍在云台上** | **被重力和绝对 yaw 对齐** | 随车**平移**，但不随车**旋转** |

`world` 这个设计是理解全局的关键：

- 它的原点跟着车走（所以从 gimbal 到 world **只有旋转、没有平移**，
  见 `solver.cpp:117`）
- 但它的**朝向固定**：z 轴对齐重力，yaw 有绝对参考

**为什么要这么设计**：因为朝向固定，敌方车在 world 系里才能用
"匀速直线运动"这么简单的模型描述。如果参考系跟着自己云台转，
那自己转一下头，敌方车在坐标里就"飞"了一下，EKF 根本没法建模。

## 变换链条：一个像素点如何变成世界坐标

```
armor 系的 4 个 3D 物点  +  图像上的 4 个像素点
        |
        |  cv::solvePnP (SOLVEPNP_IPPE)        solver.cpp:110
        v
   位姿（相对 camera 系）
        |
        |  * R_camera2gimbal + t_camera2gimbal  solver.cpp:116
        |     （手眼标定的固定外参）
        v
   xyz_in_gimbal（米）
        |
        |  * R_gimbal2world                     solver.cpp:117
        |     （电控实时回传的四元数，只旋转不平移）
        v
   xyz_in_world（米）  ->  再转球坐标 ypd_in_world   solver.cpp:133
```

前两跳靠**标定出来的固定参数**，最后一跳靠**电控实时回传的四元数**。

最后那个球坐标 `(yaw, pitch, distance)` 是给 EKF 用的：
因为相机**测角准、测距糙**，用球坐标才能在噪声矩阵里分别给它们不同的信任度。

### 内外参从哪些 yaml 键读入

| yaml 键 | 含义 | 谁生成 | 读入点 |
| --- | --- | --- | --- |
| `camera_matrix` | 3×3 内参，fx/fy/cx/cy（焦距与光心，像素） | `calibrate_camera` | `solver.cpp:66` |
| `distort_coeffs` | 5 个畸变系数（镜头把直线拍弯了） | `calibrate_camera` | 同上附近 |
| `R_camera2gimbal` | 相机到云台的旋转 | `calibrate_handeye` | `solver.cpp:58-61` |
| `t_camera2gimbal` | 相机到云台的平移 | `calibrate_handeye` | 同上附近 |

**这四个全是标定工具的生成物，不是手调参数。** 手改任何一个都会毁掉 PnP，
而且症状是"距离一直不对"这种难查的偏差，不是崩溃。
现在没相机没车，属于**别碰**清单。

### 装甲板实物尺寸（PnP 的尺子）

`solver.cpp:30-32`，单位米：

| 常量 | 值 | 实物 |
| --- | --- | --- |
| `LIGHTBAR_LENGTH` | `56e-3` | 灯条长 56mm |
| `BIG_ARMOR_WIDTH` | `230e-3` | 大板宽 230mm |
| `SMALL_ARMOR_WIDTH` | `135e-3` | 小板宽 135mm |

选错大小板 → 整个距离都错。所以 `detector` 的 `get_type` 判错，后果会一直传到弹道。

## `optimize_yaw`：为什么 PnP 之后还要暴力搜一遍

`solver.cpp:284`。做法很朴素：**在 140 度范围内，每隔 1 度试一次，
挑重投影误差最小的那个 yaw**（`SEARCH_RANGE = 140` 同时充当范围和循环次数，
`solver.cpp:288,294`）。

为什么要这么干：`SOLVEPNP_IPPE` 是专给"4 个共面点"的快速解法，
但对装甲板这种**扁平**靶（宽 230mm、高才 56mm），它给出的 yaw **有歧义** ——
板往左偏 30° 和往右偏 30° 在图像上几乎一样，数值上就分不清。

而 yaw 恰恰是反小陀螺最关键的量（它表示这块板正面朝哪），所以宁可花 140 次
重投影去暴力搜。原始 PnP 的 yaw 不会丢，存进 `armor.yaw_raw`（`solver.cpp:306`），
曲线里的 `armor_yaw_raw` 就是它，可以拖出来和 `armor_yaw` 对比看优化效果。

**一个例外**：平衡步兵（大板 + 3/4/5 号）**不做** yaw 优化，
因为它的 pitch 假设不成立（`solver.cpp:135-137`）。

## 弹道解算

`tools/trajectory.cpp`。输入输出很干净：

- 入：`v0` 弹速【m/s】、`d` 水平距离【m】、`h` 高度差【m】（目标比枪口高为正）
- 出：`pitch` 发射角【弧度】（抬头为正）、`fly_time` 飞行时间【秒】
- `unsolvable` = true 表示这一帧解不出来，**调用方必须放弃**，别拿垃圾角度去转云台

### 两级模型

| | `TrajectoryV1`（`:31`） | `TrajectoryV2`（`:75`） |
| --- | --- | --- |
| 考虑 | 只有重力 | 重力 + **空气阻力** |
| 解法 | 联立斜抛方程消去时间，套一元二次求根公式 | 分两步，见下 |
| 是否生效 | ❌ 死代码 | ✅ **实际用的是这个** |

生效的那个由 `tools/trajectory.hpp:29` 的 `using Trajectory = TrajectoryV2` 决定。

V1 的两个根对应低抛和高抛（吊射），代码取飞行时间短的那个即低抛 ——
飞得越久，预测误差和风的影响越大，所以能平射就不吊射。

### V2 的求解思路（两步，不耦合）

1. **只看水平方向**。有阻力时水平速度衰减，由此列出关于飞行时间的二次方程，
   先定 `fly_time`（`trajectory.cpp:96-129`）。
2. **飞行时间已知**，代入竖直方程反解发射角（`:136`）：
   ```
   target_sin_angle = (h + 0.5 * g * fly_time²) / (v0 * fly_time)
   ```
   抬枪既要够到 `h`，还要额外补上这段时间里重力造成的下坠 `0.5*g*t²` ——
   这就是"枪口得往上抬一点"的来源。

这样避免把阻力和重力耦合成一个超越方程；代价是竖直方向仍只算了重力（近似）。

两处判 `unsolvable`：判别式 < 0（子弹在到达该距离前已被阻力减速殆尽），
以及 `|sin| > 1`（再怎么抬枪也够不着）。

### 不在 yaml、改了要重新编译

| 常量 | 值 | 位置 |
| --- | --- | --- |
| 重力 `g` | `9.7833` | `trajectory.cpp:20`（不是课本的 9.8，按本地纬度/海拔修正过） |
| `kBig` / `mBig` | `0.000429838` / `0.043` | `trajectory.cpp:70-71`，大弹丸 42mm |
| `kSmall` / `mSmall` | `0.000067165` / `0.0032` | `trajectory.cpp:72-73`，小弹丸 17mm |

### ★一个容易读反的地方

`trajectory.cpp:82`：

```cpp
bool isBigBullet = v0 > 18 ? false : true;
```

**弹速 > 18 m/s 判为「小」弹丸**。依据是规则限速不同：17mm 小弹丸允许 30 m/s 上下，
42mm 大弹丸只有 16 m/s 左右，所以弹速本身就能区分兵种。

隐患：若弹速回传异常（例如 C 板还没测到、回传 0），会误判成大弹丸而用错 `k/m`。

## 飞行时间迭代（打提前量的核心）

子弹飞过去要时间，这期间敌方车还在动，所以必须**打提前量**。
但"预测多久之后"取决于飞行时间，而飞行时间又取决于打哪儿 —— 这是个循环依赖。

解法是迭代（`tasks/auto_aim/aimer.cpp:84-120`）：

```
用当前位置算一次弹道           -> fly_time_0        aimer.cpp:70
循环最多 10 次：
    用 EKF 把目标预测到 future + fly_time  -> 新位置    aimer.cpp:87-88
    用新位置重算弹道                       -> 新 fly_time  aimer.cpp:103
    两次 fly_time 差 < 0.001 秒就收敛，break            aimer.cpp:114
```

收敛后用最终位置算 yaw/pitch，并加上 `yaw_offset_` / `pitch_offset_`
（`aimer.cpp:124-125`）。注意 `pitch` 取了负号，因为**世界坐标系下 pitch 向上为负**。

## 阶段 4 完成标准

- [ ] 能画出"像素 -> camera -> gimbal -> world"的变换链条，并说出每跳靠什么参数
- [ ] 能解释 `world` 系为什么"随车平移但不随车旋转"
- [ ] 知道 `camera_matrix` 这类标定产物为什么不能手改
- [ ] 能说出 `optimize_yaw` 存在的原因（IPPE 对扁平靶的 yaw 歧义）
- [ ] 知道实际生效的是 `TrajectoryV2`，以及 `v0 > 18` 判的是小弹丸

---

# 阶段 5：跟踪与 EKF

**这是全仓库最难的部分，但顺序对了就不难。**

**先看现象，后看公式。** 不要先去读状态方程 —— 先在 PlotJuggler 里
看着曲线跳动建立直觉，再回头看代码，会省掉大量困惑。

## 第一步：先用 PlotJuggler 看懂曲线在说什么

**这一步不要跳过。** 照下面做完再往下读。

```bash
# 一个终端
plotjuggler        # UDP Server, port 9870, JSON（配置见阶段 0）

# 另一个终端
cd build
./auto_aim_test
```

拖出这几条曲线（输出代码在 `tests/auto_aim_test.cpp:199-221`）：

| 曲线名 | 对应 EKF 状态 | 含义 | 单位 |
| --- | --- | --- | --- |
| `x` | `x[0]` | 整车**旋转中心**世界系 x | 米 |
| `vx` | `x[1]` | 旋转中心 x 方向速度 | m/s |
| `y` / `vy` | `x[2]` / `x[3]` | 同上，y 方向 | 米 / (m/s) |
| `z` / `vz` | `x[4]` / `x[5]` | 同上，高度 | 米 / (m/s) |
| `a` | `x[6]` | = 状态 `yaw`，**整车朝向** | 度（乘了 57.3） |
| `w` | `x[7]` | = 状态 `vyaw`，**整车自转角速度** | rad/s（**没转度**） |
| `r` | `x[8]` | 基础半径：旋转中心到 0、2 号板的距离 | 米 |
| `l` | `x[9]` | = 状态 `r_`，半径补偿量 | 米 |
| `h` | `x[10]` | = 状态 `z_`，高度补偿量 | 米 |
| `armor_yaw` | — | 单块板 PnP 解出的朝向（优化后） | 度 |
| `armor_yaw_raw` | — | 同上，但**优化前**。和上面对比看 `optimize_yaw` 效果 | 度 |
| `nis` | — | 新息检验统计量，见阶段 6 | — |
| `last_id` | — | 上次匹配上的板编号，用来对照 `r`/`l` 该取哪个 | — |

> **曲线名和状态名不一致，别被绕晕**：曲线里 `x[9]`/`x[10]` 仍沿用旧名 `l` 和 `h`，
> `yaw`/`vyaw` 则叫 `a` 和 `w`。看图时按上表对照。

### 三种情况下该看到什么

| 目标状态 | `x`/`y` | `vx`/`vy` | `a`（yaw） | `w`（vyaw） |
| --- | --- | --- | --- | --- |
| **静止正对** | 基本是平的 | ≈ 0 | 基本不变 | ≈ 0 |
| **平移**（横穿） | 单调变化 | 稳定的非零值 | 基本不变 | ≈ 0 |
| **小陀螺**（原地旋转） | 基本是平的 | ≈ 0 | **持续单调爬升/下降**（过 ±π 会跳变） | **稳定的非零值**，这是关键特征 |

盯住一件事：**`x`/`y` 在小陀螺时应该基本不动。**

如果 `x`/`y` 在小陀螺时也跟着画圈，说明滤波器没把"整车中心"和"装甲板"分开 ——
而这正是理解下一节的钥匙。

## ★算法核心：EKF 跟踪的不是装甲板，是整车的旋转中心

**这一句话是整个阶段的核心。想通它，反小陀螺为什么这么写就全通了。**

`tasks/auto_aim/target.cpp` 的 11 维状态定义在 `:49-52`：

```
[0]x  [1]vx  [2]y  [3]vy  [4]z  [5]vz
[6]yaw(整车朝向)  [7]vyaw(自转角速度)
[8]r(基础半径)  [9]r_(半径补偿量)  [10]z_(高度补偿量)
```

**注意里面没有"装甲板位置"这一项。** 状态描述的是一辆**车**：
它的中心在哪、往哪走、朝哪转、转多快、板离中心多远。

### 几何核心：`h_armor_xyz`

`target.cpp:558`。**这是整个模型的几何核心**，从"旋转中心 + 板编号"算出该板的位置：

```cpp
angle = limit_rad(x[6] + id * 2π / armor_num_);   // 第 id 块板的朝向
r     = use_l_h ? x[8] + x[9] : x[8];             // 该板的半径
armor_x = x[0] - r * cos(angle);
armor_y = x[2] - r * sin(angle);
armor_z = use_l_h ? x[4] + x[10] : x[4];
```

四块板均匀分布在车周围，相邻差 90°（`2π / armor_num_`）。

**为什么要 `r_` 和 `z_` 两个补偿量**：真车的四块板不是完美对称的 ——
1、3 号板（`use_l_h` 那两块）的半径和高度与 0、2 号板略有差别。
所以给它们额外加一个补偿量，让模型贴合实际车。

这样一来：

- 车在原地小陀螺 → 中心 `x[0]`/`x[2]` 不动，只有 `yaw` 在变 → **中心稳定**
- 观测到的是装甲板（会画圈），但状态估计的是中心（不动）
- 于是可以稳稳瞄着中心，或者预测某块板什么时候转到正面

这就是**反小陀螺**的原理。如果状态直接跟"当前那块板"，板一换就得重新起跳，
根本没法预测。

## 过程模型（预测）：匀速假设

`target.cpp:149` 的 F 矩阵就是最朴素的匀速模型：

```
x  += vx * dt      y  += vy * dt      z  += vz * dt
yaw += vyaw * dt
r, r_, z_ 不变
```

也就是**假设"位置匀速直线、自转匀速"**。真车当然会加速，这个差异由过程噪声 Q 吸收。

### Q 的运动状态分支（一个当前无效的设计）

`target.cpp:155-193` 有个运动状态机，三个状态
`TRANSLATION` / `IN_PLACE_ROTATION` / `TRANSLATION_ROTATION`，迁移逻辑在 `:113-146`。

**但三个分支的 `v1`/`v2` 取值完全相同（都是 100/400，见 `:182-191`）**，
所以这个状态机对 Q **实际没有任何影响**。想按运动模式调 Q 的话，
就是改这三个分支的地方。

`motion_state_` 并非全无用处：`update_ypda` 里旋转态会把 `r2_angle` 放大 3 倍
（`target.cpp:449-451`），那处是活的。

## 观测模型：为什么用球坐标

`target.cpp:489` 附近。观测量是 4 维：

```
车心状态 --h_armor_xyz--> 第 id 块板的 xyz --xyz2ypd--> (yaw, pitch, distance) + 板朝向角
```

**为什么不用 xyz 直接当观测**：因为相机的误差不是各向同性的 ——
**测角很准，测距很糙**（远处差几十厘米很正常）。
用球坐标才能在噪声矩阵 R 里分别给它们不同的信任度。

### R 的启发式构造

`target.cpp:444-480`：

| 分量 | 值 | 含义 |
| --- | --- | --- |
| `r2_azimuth` / `r2_pitch` | 固定 `4e-3` | 很小 = **很信任**，因为测角准 |
| `r2_d` | `log(|delta_angle|+1) + 1` | 距离噪声，随"板正面偏离视线的角度"增大而增大 |
| `r2_angle` | `log(|distance|+1)/200 + 9e-2` | 板朝向角的噪声，**随距离增大而增大**（越远 yaw 越不可信） |

两处特殊放大：

- 旋转态额外把 `r2_angle` 放大 3 倍（`:449-451`）——
  小陀螺时板朝向变化最快，最不该被信任
- 换镜头瞬态时把噪声放大到 `4e+4` / `×300`（`:475` 附近）——
  切长焦/短焦的瞬间画面完全变了，这几帧的观测干脆不要信

## 数据关联：这一帧看到的板，是四块中的哪一块？

这是跟踪的核心难题。用**马氏距离**（`target.cpp:313-353`）。

### 马氏距离 = "考虑了不确定性的距离"

普通欧氏距离把每个方向一视同仁；马氏距离先用协方差 `S` 做归一化，于是：

- "在本来就不确定的方向上差一点" → 惩罚**小**
- "在很确定的方向上差一点" → 惩罚**大**

这正是我们要的：距离测不准，所以距离差一点可以容忍；角度测得准，角度差一点就很可疑。

`S = H*P*H' + R` = "预测的不确定性 + 观测的不确定性"，即残差本该有多大的方差。

做法：对四块板各算一次马氏距离，取最小的那块作为匹配结果。

### 卡方门限与迟滞防抖

`target.cpp:363-364`：

```cpp
double CHI_SQ_THRESHOLD = 9.488;   // 自由度 4、置信度 95% 的卡方分位点
double HYSTERESIS_MARGIN = 5.0;
```

`9.488` 的含义：观测是 4 维所以自由度取 4；如果观测真的属于这块板，
马氏距离超过 9.488 的概率只有 5%，超了就认为"这不是同一块板"。
**★这个值写死在代码里，不在 yaml。**

**迟滞（Hysteresis）为什么必需**：小陀螺时两块板的马氏距离可能非常接近，
如果每帧都取最小值，匹配 ID 会在两块板之间来回跳（0→1→0→1），
每次跳都被当成"换板"事件，EKF 的 yaw 也跟着来回拽，跟踪就废了。

做法：只要上一次那块板"还算合理"（马氏距离 < 9.488），
就要求新候选必须**明显更好**（好过它至少 5.0）才允许换，否则维持原判。

**这和你在电控里给按键/阈值加迟滞回环是同一个套路。**

## Tracker 四态状态机

`tracker.cpp:324` 的 `state_machine(bool found)`。决定"这一帧到底算不算跟上了目标"：

```
        ┌──────────────── lost ◄──────────────┐
        │ found                               │ 连续丢 > max_temp_lost_count
        v                                     │
   detecting ──── 连续 found ≥ min_detect_count ──► tracking ──► temp_lost
        │                                              ▲            │
        └─ 中断则回 lost                                └── found ───┘
```

| 状态 | 含义 |
| --- | --- |
| `lost` | 没有目标 |
| `detecting` | 疑似发现，还在攒证据 |
| `tracking` | 确认跟上了，这时才输出目标 |
| `temp_lost` | 短暂丢失（转背面/被遮挡），**EKF 继续预测**，不立刻放弃 |

关键迁移在 `tracker.cpp:339`（`detect_count_ >= min_detect_count_`）
和 `:371`（`temp_lost_count_ > max_temp_lost_count_`）。

### 这两个参数怎么调（离线可验证）

| 参数 | demo.yaml | 调大 | 调小 |
| --- | --- | --- | --- |
| `min_detect_count` | 5 | 不容易被误识别骗到，但**发现目标慢半拍** | 反应快，但容易锁到误识别上 |
| `max_temp_lost_count` | 11 | 转背面/被短暂遮挡时不丢跟踪，**小陀螺跟得更连贯**；但目标真跑了也会多跟一会儿 | 丢得快，小陀螺容易断 |

前哨站单独用一套更大的值 `outpost_max_temp_lost_count: 75`
（`demo.yaml:38`，切换逻辑 `tracker.cpp:367-369`），
因为它转速固定且会长时间只露侧面。

### P0：不同兵种给不同初值

`tracker.cpp:389` 的 `set_target` 按兵种分支给 P0（初始协方差）：

```cpp
Eigen::VectorXd P0_dig{{1, 64, 1, 64, 1, 64, 0.4, 100, 1, 1, 1}};   // tracker.cpp:401
```

P0 表示"一开始我有多不确定"。注意 `vyaw` 那一维给到 **100**（很不确定）——
刚看到一辆车时完全不知道它转不转，所以给个大值让滤波器快速去学。

**★P0 写死在代码里，不在 yaml**（`tracker.cpp:401,407,413,419` 四个兵种分支）。

## 阶段 5 完成标准

- [ ] 在 PlotJuggler 里看过三种运动形态的曲线，能说出小陀螺的特征是 `w` 非零而 `x`/`y` 平
- [ ] 能解释"EKF 跟的是整车旋转中心，不是装甲板"以及这对反小陀螺意味着什么
- [ ] 能说出 `h_armor_xyz` 干什么、为什么需要 `r_`/`z_`
- [ ] 知道观测为什么用球坐标而不是 xyz
- [ ] 能解释迟滞机制解决的是什么问题
- [ ] 调过 `min_detect_count` / `max_temp_lost_count` 并观察到跟踪连贯性的变化

---

# 阶段 6：收口

## 动手练习：NIS 阈值

这是三处疑似 bug 里**唯一离线能动手的**一处。

### 问题在哪

`tools/extended_kalman_filter.cpp`。NIS（Normalized Innovation Squared）是
"滤波器自检"：如果模型和噪声设置正确，NIS 应该服从自由度 4 的卡方分布。
超过门限太多次，说明滤波器已经不可信。

原来的代码写的是 `0.711`，而注释声称要 95% 分位。**但 `0.711` 是 5% 分位，
`9.488` 才是 95% 分位** —— 两者含义完全相反：

- 用 `9.488`：NIS 超过它才算异常，正常数据只有 5% 会误报 ✅
- 用 `0.711`：正常数据有整整 95% 都会超过，等于把健康跟踪判成异常 ❌

配合 `tracker.cpp:113` 的健康检查（最近 100 次里失败率 ≥ 40% 就丢跟踪，
`window_size = 100` 见 `extended_kalman_filter.hpp:43`），理论上会**误杀健康跟踪**。

**现已改为 `9.488`**（`extended_kalman_filter.cpp:81-82`）。
`7.779`（90% 分位）作为更严格的备选保留在下面的注释里。

### ★实测结论：在 demo 素材上这个 bug 不暴露

这一点很重要，**不要以为改完就能看到明显变化**。实测数据：

| | `Bad Converge Found` 次数 | `Target diverged` 次数 |
| --- | --- | --- |
| 改前（`0.711`），201 帧 | **0** | 0 |
| 改后（`9.488`），201 帧 | **0** | 0 |

行为完全一致。加临时探针测了 NIS 的真实分布（113 次 EKF 更新）：

- 中位数 **0.009**，p90 **0.128**，最大 **1.966**
- 超过 `0.711` 的只有 **3 次**，超过 `9.488` 的 **0 次**

原因有两层：

1. **demo 这段视频目标基本正对、运动平缓**，本来就不容易触发；
   而且"最近 100 次里失败 40%"这个门槛远远没达到（3/113 ≈ 2.7%）。
2. **更根本的**：`extended_kalman_filter.cpp` 里 NIS 用的是**后验残差** ——
   `:52` 先更新了 `x`，`:55` 才算 `residual = z - h(x)`，此时 `x` 已经是更新后的了，
   不是定义上的**新息**（应该用更新前的 `x_prior` 算）。所以数值系统性偏小。

第 2 点**没有改** —— 改它要动滤波器的计算顺序，影响面远大于换个常数。
只在代码注释里记录了（`extended_kalman_filter.cpp:77-80`）。

### 这本身是个好教学点

**"改对了但看不出差别"** 是调参里最常见的陷阱。它说明：

- 阈值错误只在**被触发时**才有害。素材不触发，就看不出差别。
- 想验证这个修复，需要**小陀螺 / 快速平移**的素材，让 NIS 真的变大。
- 所以"我改了没变化"不等于"改错了"，也不等于"改得没用"。

自己复现一遍：

```bash
cd build
./auto_aim_test -s=0 -e=200 2>&1 | grep -c "Bad Converge Found"
```

## ★全局调参手册（三张表）

这三张表是本文最实用的部分。**调参前先确认参数在哪张表里。**

### 表 1：离线能调，且能看到效果

配置在 `configs/demo.yaml`，改完**不用重新编译**（yaml 是运行时读的），直接重跑。

| 参数 | 值 | 管什么 | 怎么看效果 |
| --- | --- | --- | --- |
| `threshold` | 150 | 二值化阈值 | `binary_img` 窗口 + `armor_num` 曲线 |
| `min_lightbar_ratio` | 1.5 | 灯条最小长宽比 | `armor_num` |
| `max_lightbar_ratio` | 20 | 灯条最大长宽比 | `armor_num` |
| `min_lightbar_length` | 8 | 灯条最小长度（像素） | 远处目标是否还认得出 |
| `min_armor_ratio` | 1 | 板长宽比下限 | `armor_num` |
| `max_armor_ratio` | 5 | 板长宽比上限 | `armor_num` |
| `max_side_ratio` | 1.5 | 长短灯条长度比 | 侧对时是否还认 |
| `max_angle_error` | 45°（`/57.3`） | 灯条偏离竖直的容忍度 | 车倾斜时是否还认 |
| `max_rectangular_error` | 25°（`/57.3`） | 灯条与连线夹角偏差容忍度 | 误配对数量 |
| `min_confidence` | 0.8 | 数字分类置信度下限 | `not_armor` 是否被滤掉 |
| `min_detect_count` | 5 | 攒几帧才确认跟踪 | `tracker` 状态、发现目标的延迟 |
| `max_temp_lost_count` | 11 | 丢几帧才判真丢 | 小陀螺跟踪连贯性 |
| `outpost_max_temp_lost_count` | 75 | 前哨站专用 | 同上 |
| `roi` / `use_roi` | `false` | 是否只在感兴趣区域里找 | 帧率 + 边缘目标是否丢 |
| `yolo_name` | `yolov5` | 用哪个 YOLO 后端 | 识别效果与耗时 |
| `enemy_color` | `"blue"` | 敌方颜色 | 改成 `"red"` 应该一个都认不出 |

### 表 2：离线不能验证，现在别碰

| 参数 | 位置 | 为什么不能碰 |
| --- | --- | --- |
| `exposure_us` | `demo.yaml:66` | **视频是录好的**，改了对回放毫无影响 |
| `gain` | `demo.yaml:67` | 同上 |
| `gamma` / `img_gamma` | `demo.yaml:68-69` | 同上 |
| `yaw_offset` | `demo.yaml:41` | 要看**实弹落点**才能标 |
| `pitch_offset` | `demo.yaml:42` | 同上 |
| `far_pitch_offset` | `demo.yaml:48` | 同上 |
| `high_speed_delay_time` | `demo.yaml:46` | 要标全链路延迟，离线无法闭环 |
| `low_speed_delay_time` | `demo.yaml:47` | 同上 |
| `camera_matrix` | 读入 `solver.cpp:66` | **标定工具生成物**，手改即毁 PnP |
| `distort_coeffs` | 读入 `solver.cpp:67` | 同上 |
| `R_camera2gimbal` | 读入 `solver.cpp:58` | 手眼标定生成物，同上 |
| `t_camera2gimbal` | 读入 `solver.cpp:59` | 同上 |
| `R_gimbal2imubody` | 读入 `solver.cpp:57` | IMU 安装偏差，标定生成物 |
| `Q_yaw` / `R_yaw` | `demo.yaml:121-122` | 是 **MPC 的代价权重**，不是 EKF 的 Q/R；而且只走主程序的 `Planner`，`auto_aim_test` 根本不经过 |
| `Q_pitch` / `R_pitch` | `demo.yaml:125-126` | 同上 |

`Q_yaw` 这一条最容易搞错：名字看着像卡尔曼滤波的 Q，**其实是 MPC 的权重**，
而且离线链路压根不经过 `Planner`。想调 EKF 的 Q 看表 3。

### 表 3：不在 yaml，改了要重新编译

新人最容易在这里"找错地方"——在 yaml 里翻半天找不到。

| 内容 | 位置 | 说明 |
| --- | --- | --- |
| EKF 的 **P0**（初始协方差） | `tracker.cpp:401,407,413,419` | 四个兵种分支各一套，11 维 |
| EKF 的 **R**（观测噪声） | `target.cpp:444-480` | 启发式构造，随距离/角度变化 |
| EKF 的 **Q**（过程噪声） | `target.cpp:182-191`（`v1`/`v2`）、`:201-217`（组装） | 三个运动状态分支当前取值相同 |
| 卡方门限 `CHI_SQ_THRESHOLD` | `target.cpp:363` | `9.488`，数据关联用 |
| 迟滞裕量 `HYSTERESIS_MARGIN` | `target.cpp:364` | `5.0` |
| NIS / NEES 阈值 | `extended_kalman_filter.cpp:81-82` | `9.488`，本次已修正 |
| NIS 健康检查窗口 | `extended_kalman_filter.hpp:43` | `window_size = 100` |
| NIS 失败率门限 | `tracker.cpp:113,219,291` | `0.4`（40%），三个 track 方法各一处 |
| 大小板判定阈值 | `detector.cpp:378,385` | `3.0` / `2.5` |
| 重力 `g` | `trajectory.cpp:20` | `9.7833` |
| 弹丸阻力系数 | `trajectory.cpp:70-73` | `kBig/mBig/kSmall/mSmall` |
| 大小弹丸判定弹速 | `trajectory.cpp:82` | `v0 > 18` 判**小**弹丸 |
| `optimize_yaw` 搜索范围 | `solver.cpp:288` | `SEARCH_RANGE = 140`（度） |
| 装甲板实物尺寸 | `solver.cpp:30-32` | 56mm / 230mm / 135mm |
| 串口波特率 | `gimbal.cpp:24` | `460800`（USB-CDC，其实无所谓） |
| `plan_thread` 周期 | `rb_auto_aim_debug.cpp` | 固定 5ms（200Hz） |
| PlotJuggler 端口 | `plotter.hpp:15` | `9870` |

## 三处疑似 bug 汇总

| # | 问题 | 状态 | 为什么 |
| --- | --- | --- | --- |
| 1 | `.vscode/tasks.json` 的 "run range" 用了 `-s ` 空格形式 | ✅ **已修** | 一处改动，立刻可验证 |
| 2 | NIS 阈值 `0.711` 与注释声称的 95% 分位矛盾 | ✅ **已修为 `9.488`** | 离线可验证（但 demo 素材不暴露，见上） |
| 3 | 发送侧 CRC16 从未计算 | ❌ **只读不改** | 要电控侧固件配合，离线做不了闭环 |

另外两处**只记录、不改**（都需要接 C 板才能验证）：

- `enemy_color == "auto"` 时红蓝映射相反（`tracker.cpp:61` vs `:136`）
- `Armor::priority` 从未被赋值，"优先级"形同虚设

## 等有硬件再做

现在没相机没车，下面这些只能等。**入口程序都在这里，到时候照着找。**

| 要做的事 | 入口程序 | 前置条件 |
| --- | --- | --- |
| 相机能不能出图、调焦 | `test_simple` | 真相机 + 真 SDK |
| 采集标定数据（图 + 四元数） | `capture` | 相机 + 云台能动 |
| 挑掉角点没识别好的图 | `calibrate_input` | 已采集的数据 |
| **相机内参标定** | `calibrate_camera` | 标定板 + 采集好的图 |
| **手眼标定**（相机相对云台的安装偏差） | `calibrate_handeye` | 内参已标好 + 车能动 |
| 手眼标定另一种解法 | `calibrate_robotworld_handeye` | 同上 |
| 弹道偏移标定（`yaw_offset`/`pitch_offset`） | 上场主程序 `rb_auto_aim_debug` | 实弹 + 能看落点 |
| 延迟标定（`*_delay_time`） | 同上 | 实弹 + 高速相机或反复试射 |
| 验证 crc16 修复 | 同上 | 电控侧固件配合 + 逻辑分析仪 |
| 验证红蓝映射哪个对 | 同上 | 接 C 板，看 `enemy_color` 实际值 |

**注意**：本机 `libgxiapi.so` 是占位库，上车前必须先去大恒官网装真 SDK
（见 `README.md` 的 3.2 节）。

## 阶段 6 完成标准

- [ ] 能解释 NIS 阈值为什么错、以及为什么改完在 demo 上看不出差别
- [ ] 三张调参表能对上号：拿到一个参数名，知道它属于哪一张表
- [ ] 知道 `Q_yaw` 不是 EKF 的 Q
- [ ] 知道三处 bug 哪些改了、哪些没改、为什么

---

# 附：全仓库速查

| 我想找… | 去哪 |
| --- | --- |
| 编译运行、快捷键、报错 | `怎么编译运行.md` |
| 依赖安装、9 个程序的用法 | `README.md` |
| 通信包结构 | `io/gimbal/gimbal.hpp` |
| 识别参数 | `tasks/auto_aim/detector.cpp` 构造函数 |
| 坐标变换 / PnP | `tasks/auto_aim/solver.cpp` |
| EKF 状态定义 | `tasks/auto_aim/target.cpp:49-52` |
| 跟踪状态机 | `tasks/auto_aim/tracker.cpp:324` |
| 弹道 | `tools/trajectory.cpp` |
| 离线链路全貌 | `tests/auto_aim_test.cpp` |
| 上场链路全貌 | `src/rb_auto_aim_debug.cpp` |
